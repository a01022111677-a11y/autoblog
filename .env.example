# 집PC 중계서버용 설정. 이 파일을 복사해 이름을 .env 로 바꾼 뒤 값만 채우세요.
# (메모장 → 다른 이름으로 저장 → 파일 이름에 ".env" 따옴표까지 입력)
# ⚠️ .env는 절대 깃허브에 올리면 안 됩니다 (.gitignore에 등록済)
TOSS_ACCESS_KEY=
TOSS_SECRET_KEY=
TOSS_PUBLISHER_ID=
RELAY_SECRET=여기에아주긴랜덤문자열
